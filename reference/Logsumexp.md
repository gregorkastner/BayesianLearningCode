# The Log-sum-exp function and the Log-mean-exp function

Numerically stable variant to compute the log of the sum (mean) of
exponentials

## Usage

``` r
logsumexp(x)

logmeanexp(x)
```

## Arguments

- x:

  sequence of real numbers

## Value

a single real number
